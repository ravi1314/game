import 'package:flutter/material.dart';

class Mybird extends StatelessWidget {
  const Mybird({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('asset/image/bird.png');
  }
}